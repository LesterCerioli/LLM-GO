

tensorflow==2.17.1          # Python 3.12 wheel confirmado (cp312-manylinux)
keras==3.5.0                # compatível com TF 2.17.x

# ---------------------------------------------------------------------------
# HuggingFace ecosystem
# ---------------------------------------------------------------------------
tokenizers==0.19.1
datasets==2.20.0
huggingface-hub==0.24.5
transformers==4.44.0
evaluate==0.4.2
accelerate==0.33.0
safetensors==0.4.4

# ---------------------------------------------------------------------------
# Numerical / scientific
# ---------------------------------------------------------------------------
numpy==1.26.4          # TF 2.17.x exige numpy<2; 1.26.x é o pino seguro
scipy==1.14.0
scikit-learn==1.5.1
pandas==2.2.2

# ---------------------------------------------------------------------------
# Data collection & GitHub API
# ---------------------------------------------------------------------------
requests==2.32.3
PyGithub==2.3.0
GitPython==3.1.43

# ---------------------------------------------------------------------------
# Tokenizer utilities
# ---------------------------------------------------------------------------
sentencepiece==0.2.0
regex==2024.7.24       # fast regex used by tokenizers internally

# ---------------------------------------------------------------------------
# Code parsing — tree-sitter for Go AST (optional, used by advanced tooling)
# tree-sitter 0.22.6 was never released; 0.22.3 is the last 0.22.x build.
# Core training uses regex-based tagging and does NOT require tree-sitter.
# Uncomment to enable AST-level structural tagging:
# tree-sitter==0.22.3
# tree-sitter-languages==1.10.2
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# CLI & configuration
# ---------------------------------------------------------------------------
click==8.1.7
pyyaml==6.0.2
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# Logging & progress
# ---------------------------------------------------------------------------
rich==13.7.1
tqdm==4.66.5
tensorboard==2.17.1         # deve acompanhar a versão do TF

# ---------------------------------------------------------------------------
# Metrics
# ---------------------------------------------------------------------------
rouge-score==0.1.2
sacrebleu==2.4.2

# ---------------------------------------------------------------------------
# Deduplication (production-grade MinHash LSH)
# ---------------------------------------------------------------------------
datasketch==1.6.5
xxhash==3.4.1

# ---------------------------------------------------------------------------
# Dev / testing (also in pyproject.toml [dev])
# ---------------------------------------------------------------------------
pytest==8.3.2
pytest-cov==5.0.0
black==24.8.0
ruff==0.5.7
mypy==1.11.1
pre-commit==3.8.0
